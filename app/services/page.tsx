import services from '@/data/services.json';export default function Services(){return <main className="container" style={{padding:'55px 20px'}}><h1>Painting Services in Dubai</h1><p>Explore our complete range of professional painting and surface finishing services.</p><div className="grid">{services.map(x=><a className="card" href={`/services/${x.slug}`} key={x.slug}><h2 style={{fontSize:20}}>{x.name}</h2><p>{x.description}</p><b>{x.price}</b></a>)}</div></main>}
