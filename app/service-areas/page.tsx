import Link from 'next/link';import areas from '@/data/areas.json';export default function Areas(){return <main className="container" style={{padding:'55px 20px'}}><h1>Painting Service Areas in Dubai</h1><p>Serving {areas.length} communities with reliable painters and 24/7 online booking.</p><div className="grid">{areas.map(x=><Link className="card" href={`/service-areas/${x.slug}`} key={x.slug}><h2 style={{fontSize:20}}>{x.name}</h2><p>{x.description}</p></Link>)}</div></main>}
