import services from '@/data/services.json';import areas from '@/data/areas.json';import {notFound} from 'next/navigation';
export function generateStaticParams(){return areas.map(x=>({area:x.slug}))}export default function Area({params}:{params:{area:string}}){let a=areas.find(y=>y.slug===params.area);if(!a)return notFound();return <main className="container" style={{padding:'55px 20px'}}><p><a href="/service-areas">Service Areas</a> / {a.name}</p><h1>Painting Services in {a.name}</h1><p style={{fontSize:19}}>{a.description} We serve apartments, villas, offices and commercial properties with careful preparation and dependable local response.</p><h2>Services available in {a.name}</h2><div className="grid">{services.map(x=><a className="card" href={`/service-areas/${a.slug}/${x.slug}`} key={x.slug}>{x.name}</a>)}</div><p style={{marginTop:30}}><a className="btn" href="/contact">Book a Local Painter</a></p></main>}
