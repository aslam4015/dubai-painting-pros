import services from '@/data/services.json';import areas from '@/data/areas.json';import s from '@/data/settings.json';
export default function Home(){return <><main><section style={{background:`linear-gradient(110deg,${s.colors.primary},#245b83)`,color:'white',padding:'100px 0'}}><div className="container"><p style={{color:s.colors.amber,fontWeight:700}}>DUBAI PAINTING PROS</p><h1 style={{fontSize:'clamp(38px,7vw,72px)',maxWidth:800,margin:'12px 0'}}>Dubai’s Trusted Painting Experts</h1><p style={{fontSize:20,maxWidth:650}}>Complete interior, exterior, residential and commercial painting services across Dubai.</p><a className="btn" href="/contact">Book a 24/7 Free Quote</a></div></section><section className="container" style={{padding:'60px 20px'}}><h2>Complete Painting Services in Dubai</h2><p>From a single door to a full villa, our professional painters deliver clean preparation, quality materials and a smooth finish.</p><div className="grid">{services.slice(0,12).map(x=><a className="card" href={`/services/${x.slug}`} key={x.slug}><h3>{x.name}</h3><p>{x.description}</p></a>)}</div><p style={{marginTop:25}}><a className="btn" href="/services">View all 45 services</a></p><h2 style={{marginTop:65}}>Areas We Serve</h2><div className="grid">{areas.map(x=><a className="card" href={`/service-areas/${x.slug}`} key={x.slug}>{x.name}</a>)}</div></section></main></>}
