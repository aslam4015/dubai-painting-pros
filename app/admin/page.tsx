import services from '@/data/services.json';import areas from '@/data/areas.json';export default function Admin(){return <main className="container" style={{padding:'55px 20px'}}><h1>Admin Dashboard</h1><p>This starter dashboard reads from the project JSON data files.</p><div className="grid"><div className="card"><h2>{services.length}</h2><p>Services</p></div><div className="card"><h2>{areas.length}</h2><p>Areas</p></div><div className="card"><h2>{services.length*areas.length}</h2><p>Combination pages</p></div><div className="card"><h2>0</h2><p>Leads awaiting database connection</p></div></div><p>For production, connect this area to authentication and a database before public launch.</p></main>}
