'use client';import {useForm} from 'react-hook-form';import s from '@/data/settings.json';export default function Contact(){let {register,handleSubmit}=useForm();return <main className="container" style={{padding:'55px 20px',maxWidth:850}}><h1>Get a Free Painting Quote</h1><p>Book online 24/7. Our team will contact you to confirm your requirements.</p><form className="card" onSubmit={handleSubmit(()=>alert('Thank you. Your request has been received.'))} style={{display:'grid',gap:14}}>{['name','phone','service','area','propertyType','preferredDateTime','message'].map(f=><label key={f}>{f.replace(/([A-Z])/g,' $1')}<input {...register(f)} required={f!=='message'} style={{display:'block',width:'100%',padding:13,marginTop:5,border:'1px solid #ddd',borderRadius:7}} /></label>)}<button className="btn" type="submit">Request Free Quote</button></form><p>Phone: {s.phone} · WhatsApp: {s.whatsapp} · {s.email}</p></main>}
